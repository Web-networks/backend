events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://frontend:80;
        }

        location /restapi {
            proxy_pass http://backend:5050;
        }
    }
}